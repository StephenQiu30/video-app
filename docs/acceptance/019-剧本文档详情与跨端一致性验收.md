# 019 剧本文档详情与跨端一致性验收

- [x] App 文档列表整行可点击并提供“打开剧本文档详情”辅助功能提示。
- [x] App 详情支持列表进入、深链接进入、安全返回、刷新、删除和解析中自动轮询。
- [x] App 与 Web 均展示同一份文档元数据、正文和完成后的剧本分析结果。
- [x] 窄屏指标不再截断 `FOUNTAIN`，“角色 162”已修正为“字符 162”。
- [x] 已完成/失败分析不再误显示“等待调度”。
- [x] Web 同源上传代理仅转发到配置中的预签名 MinIO 端点。
- [x] 服务端兼容导入 Worker 的 `scenes + parse_summary` 元数据。
- [x] 未修改 `.env`、`.env.prod` 或其他环境配置文件。
- [x] 测试账号、文档、分析任务、Outbox 事件和对象存储文件均已确认清零。
- [x] 最终 iOS Release 已使用生产 Tailscale API 完成签名构建。
- [ ] 最新 Release 安装到真机：iPhone 为 iOS 27.0 Beta，而当前 Xcode 26.6 的设备支持上限为 iOS 26.5；CoreDevice 因此报告 `ddiServicesAvailable=false` 与 `tunnelState=unavailable`。

## 2026-09-04 验收证据

- App：`flutter analyze` 无问题；`flutter test` 共 106 项通过，含 390×844、英文 200% 辅助字号下的剧本详情与完成分析回归。
- App 真实 API：iOS 模拟器 3 项集成测试通过，覆盖认证、生成客户端及 MP4/剧本上传删除。
- App 生产链路：真实剧本从文档列表进入详情，AI 分析经历排队、分析、发布并以 100% 完成，结构化结果和完整报告可见。
- 服务端：`uv run pytest -q` 为 1419 项通过、1 项按平台跳过；剧本源回归组 24 项通过。
- Web：完整前端测试 229 项通过，Lint 通过；生产环境上传请求依次返回 `201 / 201 / 200 / 200` 并进入 ready 详情。
- `agent-browser`：桌面与 390×844 移动端均展示完成分析；移动端浅色/深色无横向溢出，分析标签页可切换。
- `review-agent`：第一轮发现并修复大字号详情操作区溢出与契约说明偏差；第二轮只读复审无遗留缺陷。
- 生产重建：API、Web、导入/下载/报告 Worker 与平台 Runner 均以最终镜像重新创建并通过健康检查；Tailscale API `/health/ready` 返回 `ok`。
- 视觉证据：`/tmp/framegrab-dogfood-Tluzym/fixed/` 与 `/tmp/framegrab-dogfood-Tluzym/parity/`。
