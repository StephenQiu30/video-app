# 017 用户内容删除与 Web 文案同步验收

- [x] 冻结 OpenAPI 包含下载和剧本文档 DELETE 操作，Dart 客户端由生成器产出且无手改。
- [x] 下载记录列表和详情均使用二次确认后删除，成功刷新真实历史或安全返回。
- [x] 剧本文档列表使用二次确认后删除并刷新真实列表。
- [x] 下载与剧本文档列表默认不显示危险按钮，左滑后显示删除；手势提示、独立语义和详情低强调删除入口均保留。
- [x] 删除占用冲突显示“先结束相关分析”的可行动提示，原记录继续留在列表。
- [x] 活动下载、永久删除范围和活动剧本文档分析限制均在确认文案中明确说明。
- [x] 下载页与剧本文档页中文说明、空状态和业务状态已与 Web 当前文案一致。
- [x] 全量 `flutter analyze`、`flutter test`、Android Debug 和 iOS Simulator 构建通过。

## 验收证据

- App OpenAPI 白名单由 42 个操作增加到 44 个，新增 `deleteDownload` 与 `deleteDocument`。
- Widget 回归覆盖下载/剧本文档左滑删除、下载详情删除返回、成功后列表消失、冲突失败保留；仓库 fake 记录真实资源 ID，确认对话框取消前不会发起请求。
- `flutter analyze` 无问题，`flutter test` 当前通过 77 项；`flutter build apk --debug` 和 `flutter build ios --simulator --no-codesign` 均通过。
- iPhone 17 Pro / iOS 26.5 Simulator 对本地 `video-server:8111` 的 3 项真实认证、分析与 MP4/剧本上传集成测试通过；上传用例随后通过生成 DELETE 操作删除两个临时资源，并从真实下载/文档列表确认记录消失。
