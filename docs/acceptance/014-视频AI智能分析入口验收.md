# 014 视频 AI 智能分析入口验收

- 状态：Passed
- 日期：2026-08-31

- [x] App 专用快照只新增已评审的七个视频 Analysis operation，生成目录无手写 DTO。
- [x] 成功下载详情展示 AI 入口，并能选择 Skill、语言与可选分析重点。
- [x] 创建、恢复、轮询、取消、重试和删除使用真实生成客户端与统一 Bearer 会话。
- [x] 视觉分析与视频文章两类结构化结果可在移动端阅读。
- [x] 分析失败不改变下载成功、视频预览或文件获取能力。
- [x] 错误、重试、动态字体、语义、触控尺寸及深浅主题通过 Widget 验收。
- [x] 格式、静态分析、全部测试、真实 OpenAPI 漂移检查与 Android/iOS 构建通过。

## 验证证据

- 本机 `http://127.0.0.1:8111/openapi.json` 与视频 Skill 目录均返回 200；Homebrew 的 PostgreSQL、Redis 和 RabbitMQ 均为 started。
- 本地 Java/Maven 生成两次的快照与客户端校验和一致；生成包 `dart analyze` 与 453 项测试通过。
- App `dart format`、`flutter analyze` 与 55 项单元/Widget 测试通过。
- iPhone 17 Pro 模拟器 2 项真实服务集成测试通过；Analysis 用例使用生成客户端、Bearer 会话和真实 Skill，以不存在的下载验证受保护创建路由的 404，不触发模型调用或消耗 AI 额度。
- Android debug APK 使用 `http://10.0.2.2:8111` 构建通过；iOS Simulator App 使用 `http://127.0.0.1:8111` 构建通过。

最终结论：Passed。
