# 005 原生品牌资产与启动标识验收

- 状态：Passed
- 日期：2026-08-30

- [x] App 仓库中的 Logo 源与 Server 品牌源一致，衍生图标用途清楚。
- [x] iOS 桌面图标和启动过渡不再显示 Flutter 默认标识。
- [x] Android 传统、圆形、自适应桌面图标和启动过渡均使用“帧取”标志。
- [x] 内容页顶部仅保留“帧取 / Framegrab”文字，首页主任务层级和大字体布局不回归。
- [x] iOS 1024×1024 图标无 Alpha，所有 AppIcon 文件尺寸与 Contents.json 一致。
- [x] `dart run tool/check.dart`、Android Debug 与 iOS Simulator 构建通过。

最终结论：通过。iPhone 17e 模拟器实际启动和首页复验均显示“帧取”品牌，未出现 Flutter 默认标识；Android Debug APK 与 iOS Simulator App 均已成功构建。首页完成态截图见 `qa-output/native-auth/ios-final-home.png`。
