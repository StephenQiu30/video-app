# 006 原生认证与应用导航验收

- 状态：Passed
- 日期：2026-08-30

- [x] App 专用 OpenAPI 快照与生成客户端来自 Server 026 冻结契约，无手写平行 DTO。
- [x] 中文/英文登录与注册页面覆盖校验、提交、失败、成功和交叉切换。
- [x] 启动恢复、Access 内存边界、Refresh 安全存储、单飞刷新与退出清理通过自动化测试。
- [x] 顶部品牌/会话栏和底部五 Tab 在默认/大字体、深/浅主题下无溢出，语义完整。
- [x] iPhone 17e 390×844 模拟器完成真实注册、账户展示和退出验收。
- [x] `dart run tool/check.dart`、Android Debug 与 iOS Simulator 构建通过。
- [x] agent-browser 完成 Server Web 登录注册、账户、退出、键盘、错误与 390×844 验收，无开放 P0/P1/P2。

最终结论：通过。`dart run tool/check.dart` 的静态分析与 25 项测试通过；iOS Simulator 真实后端集成测试通过；Android Debug APK 和 iOS Simulator App 构建通过。agent-browser 发现的 1 个 Low 移动标题换行问题已修复并在同视口复验，开放问题为 0；完整记录见 Server 仓库 `qa-output/agent-browser-auth-20260830/report.md`。

## 2026-09-08 会话竞态修复验收

- AC-006-09：通过。退出与账号切换立即使旧会话操作失效；迟到的成功授权被丢弃并尽力撤销，失败回包不能清空新账号；已开始的安全存储写入与退出清理串行完成。
- 新增 9 项行为回归：刷新与退出/新登录交错、存储写入挂起、恢复期间过期、单飞刷新、旧请求成功/401、旧重放 401。修改前新增用例中 8 项失败，修复后认证目录共 18 项通过。见 [`auth_session_race_test.dart`](../../test/unit/auth/auth_session_race_test.dart) 与 [`authenticated_request_race_test.dart`](../../test/unit/auth/authenticated_request_race_test.dart)。
- 本轮 `flutter pub get` 通过且锁文件未变化；格式检查 561 个文件无变化，`flutter analyze --no-pub` 无问题，全量单元/Widget 测试 173 项通过。Android Debug、iOS Simulator 构建通过。
- 专用 iPhone 17e / iOS 26.5 模拟器对接已运行的 `http://127.0.0.1:8111`，现有 `integration_test` 3/3 通过，包含真实注册、页面数据、原生安全存储与退出，以及生成客户端的分析路由和上传流程。测试账号及关联资源已定向清理。
- 本轮结论：修复通过。竞态顺序由可控响应和内存凭据夹具验证，真实服务集成覆盖正常链路；未完成 iOS/Android 真机网络、Keychain/Keystore 故障演练或商店发布验收。

## 2026-09-08 邮箱验证注册

用户确认保留邮箱＋密码登录，注册必须验证邮箱。App 使用生成的 `sendNativeRegistrationCode` 获取验证码，再通过 `registerNativeUser` 提交 `verification_code`；验证码不持久化。界面提供发送、60 秒重发冷却、邮箱修改后清空、过期/错误和邮件不可用反馈。SMTP 由 Server 管理，App 不读取邮件凭据。

本轮验证结果单独记录；此前无邮箱验证码的 3/3 集成证据不替代本轮验证。

本轮结论：本地功能验收通过。175 项 Flutter 测试、analyze/format、Android debug 与 iOS simulator 构建通过。专用 iPhone 17 Pro / iOS 26.5 在 SMTP 捕获器＋PostgreSQL 隔离库＋真实 Valkey 会话环境下，2/2 集成通过，包含验证码注册、退出、邮箱密码再次登录和生成分析客户端。公网收件箱送达及商店发布未验证，未重建常驻服务。
