# 012 真实媒体解析与下载创建验收

- 状态：Passed
- 日期：2026-08-31

- [x] 普通公开链接真实返回媒体解析结果。
- [x] App 与 Web 均可从包含唯一 URL 的抖音分享文案中提取短链后完成解析请求。
- [x] 公众号文章候选流程由自动化测试验证必须明确选择。
- [x] 真实封面、元数据和服务端全部格式可见，格式可切换且创建入口保持可达。
- [x] 只有 downloadable 结果可创建任务，创建后进入真实详情并自动刷新活动进度。
- [x] 输入变化、重复点击、空格式、访问决策和网络/服务错误状态正确。
- [x] 代码中不存在 `InspectMediaIntent?`、契约待接入提示或手写并行 DTO。
- [x] 生成契约漂移检查、App 45 项测试、Android Debug、iOS Simulator 构建与真实服务 E2E 通过。

## 真实服务证据

- iPhone 17 Pro 使用 `https://youtu.be/BSBERY4hYSU` 返回真实 YouTube 标题、封面、`3:04` 时长和服务端格式清单。
- App 创建任务 `cdda3935-b0d0-4e97-94ec-aa1d63fa3226` 后进入详情；RabbitMQ 显示下载队列由唯一 worker 消费，最终状态为 `succeeded / 100%`。
- 对象存储制品为 `video/webm`、621,288,442 字节、184,181ms，持久化 SHA-256 长度为 64；历史页显示同一任务及真实运行进度，详情页活动状态请求按 2 秒间隔自动发出。

最终结论：通过。链接解析、任务创建、队列消费和进度读取均为真实服务链路，无占位、旧接口或兼容 fallback。
