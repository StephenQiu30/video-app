# 015 公开首页与持久主题验收

- 状态：Passed
- 日期：2026-08-31

- [x] 匿名根路由显示项目介绍，不强制跳转登录。
- [x] 登录、注册和受保护资源仍由原生认证边界隔离。
- [x] 登录成功进入下载工作区，退出后回到公开首页。
- [x] 公开首页覆盖开源、自托管、视频、文档、AI 与数据控制信息。
- [x] 公开、认证和登录后页面共享浅色/深色一键主题按钮。
- [x] 主题偏好使用非敏感本地存储，并有恢复与写入单元测试。
- [x] 格式、分析、62 项全量测试与 2 项真实服务集成测试通过。
- [x] Android Debug 与 iOS Simulator 构建通过。
- [x] Debug App 已签名、安装并启动于连接的 iPhone 16。
- [x] iPhone 17 Pro Simulator 完成 390×844 公开首页视觉检查，无内容裁切或横向溢出。

## 证据

- `dart run tool/check.dart`：Flutter 3.44.7、格式、生成、本地化、分析与 62 项测试全部通过。
- `flutter test integration_test -d 607722D8-D9FD-4C2B-AA87-E8922D91AE1D`：真实注册、退出、公开首页落点与 Analysis 客户端共 2 项通过。
- `flutter build apk --debug` 与 `flutter build ios --simulator --no-codesign` 均通过。
- `devicectl` 确认 `com.stephenqiu.framegrab` 0.1.0+1 已安装为 Developer App，并可在 iPhone 16 前台启动。

最终结论：Passed。

## 2026-09-03 Web 内容基线同步

- [x] App 首屏标语、定位说明和主操作与 Web SEO 公开页一致。
- [x] App 补齐解析、选择、执行、交付四阶段可审计链路。
- [x] 三类产品能力的标题、编号和说明与 Web 一致。
- [x] 自托管技术栈、MIT 许可、会话模型、Worker 隔离、制品授权和内容授权边界完整呈现。
- [x] 源代码与部署说明指向同一 `video-server` 内容基线。
- [x] 页面使用拆分后的原生 Flutter 组件和 ARB 文案，不使用 WebView 或重复硬编码。
- [x] 中英文 390×844、2 倍文字缩放可滚动到部署入口且无布局异常。

### 同步验收证据

- `flutter test test/widget/app/app_test.dart test/widget/app/accessibility_text_test.dart`：公开首页、认证路由、中英文 390×844 与 2 倍文字缩放等 41 项定向测试通过。
- `dart run tool/check.dart`：Flutter 3.44.7、依赖、本地化生成、格式、静态分析和 90 项全量测试全部通过。
- `flutter build apk --release`：Android Release APK 构建通过。
- `flutter build ios --release --no-codesign`：iOS Device Release App 构建通过；产物未签名，仅用于编译验收。

同步结论：Passed。

## 2026-09-21 Web / App 内容与排版同步

- [x] 页面标题、正文、数据指标、加载、空状态与错误状态使用左对齐。
- [x] App 首页能力编号、标题和说明与当前 Web 内容合同一致。
- [x] App 补齐 Web 的六组 FAQ 和完整 `/guide` 页面，登录前后都有明确入口。
- [x] 指南页覆盖 AI 分析报告、剧本文档、自托管、Web / App 选择和 Provider 可用性。
- [x] 公开指南在会话恢复期间不再被重定向到首页，受保护的管理深链恢复会话后返回原页面。
- [x] 新增界面使用 `ShadBadge`、`ShadButton` 等 `shadcn_ui` 组件，组件边界回归没有发现新的 Material 视觉控件。
- [x] iPhone 17 Pro / iOS 27 的指南页和管理分析页运行态截图无溢出，左对齐基线符合设计约定。

### 同步验收证据

- `dart format --output=none --set-exit-if-changed .`：格式检查通过。
- `flutter analyze`：静态分析通过。
- `flutter test`：全量 Widget / Unit 回归通过，包含 Web 内容合同、左对齐、大字体、组件边界和深链恢复。
- `flutter run -d 462F7C29-43EE-4AB6-BAE6-81908860CBCA`：Xcode 27 构建并在 iPhone 17 Pro / iOS 27 启动成功。
- 运行态截图：`.codex-audit/web-app-parity-2026-09-21/03-app-guide-after.png` 与 `04-app-admin-left-aligned.png`。
- `flutter build ios --simulator --no-codesign`：Flutter 3.44.7 对 Xcode 27 返回框架架构误判，同一日指定模拟器的 Xcode 构建与启动已通过；该通用构建命令不记为通过。

同步结论：Passed（通用 iOS Simulator 产物命令仍受 Flutter / Xcode 27 兼容性限制）。
