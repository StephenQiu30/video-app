# 017 持久解析与原生恢复验收

状态：代码验证通过，设备业务验收待完成。契约来自本地运行的服务端 OpenAPI（服务端 `55899cb872ceca8abfb91505361655e81578c83d`），不使用个人 Cookie 或未授权媒体。

| 编号 | 场景与期望 | 证据／结果 |
| --- | --- | --- |
| A1 | URL 与分享文案只发一次 POST，非终态可查询，未知响应只读找回同意图 | 单元覆盖丢失响应后 GET 找回、连续不明结果保持只读、后续显式重试遇 GET 404 才用同 key 提交；Widget 覆盖完整分享文案。真实网络中断待测。 |
| A2 | 页面切换／前后台／进程重建按 owner 找回原 ID；无重复接单 | 单元覆盖历史游标分页、输入清理不影响历史、queued→ready 只读观察，以及 ready inspection GET 失败后只读重试；前后台轮询代码已接入。进程重建的设备业务路径待测。 |
| A3 | 到期规格隐藏，显式更新仍用原 ID，新规格需再次确认；双击仅一 job | 单元覆盖本地到期后刷新而不创建任务、刷新 429／不明响应保留错误、取消不明响应只读恢复、旧路径过期必须重新解析；服务端唯一交接测试见 044。设备上的过期竞态、双击与文件校验待测。 |
| A4 | 退出和 A→B 切换清除旧标题、inspection、格式、直链 | 单元覆盖 A 账号的迟到创建响应不能进入 B 状态，历史记录立即隔离；真实账号切换及媒体直链待测。 |
| A5 | 文章发现、上传、历史、分析、受限内容与原生认证回归 | 完整工作树 236 项 Flutter 单元／Widget 通过；仅本任务暂存内容的隔离检出 231 项通过。`$review-agent` 两轮只读审查发现的 8 项恢复缺陷及 1 项跨意图轮询阻塞已修复并补回归。真实样本待测。 |
| A6 | 生成客户端与服务端版本一致；format、analyze、单元／Widget 与 Android／iOS 设备流程通过 | 隔离检出 `flutter analyze`、Android Debug APK 构建通过；iPhone 17 Pro／iOS 27.0 模拟器 `flutter run` 对隔离检出构建、启动、首屏检查通过。独立本机 `flutter build ios --simulator --no-codesign` 失败：Flutter `debug_unpack_ios` 将 `arm64 x86_64` 当作单一架构；远端 CI 的 iOS 模拟器构建成功。iOS／Android 真实业务流程未完成。 |

代码与冻结契约提交：`video-app` `de990ac7ce68749d9a79f0f6efdb0b4ffa845ec6`；对应 `video-server` `55899cb872ceca8abfb91505361655e81578c83d`。生成客户端未手改；提交后 `dart run tool/openapi.dart --from-snapshot --check` 通过。[App 远端 CI 35798904423](https://github.com/StephenQiu30/video-app/actions/runs/35798904423) 成功，包括 Android Debug APK 与 iOS 模拟器构建。模拟测试不能代替真实下载、文件大小／哈希和设备恢复；本项保持未验收。
