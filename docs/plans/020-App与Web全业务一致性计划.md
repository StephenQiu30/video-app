# App 与 Web 全业务一致性计划

状态：Complete；验证范围以验收文档为准。

2026-09-13 手机连接故障增量（进行中）：增加无凭据真机连通测试，定位实际传输失败；修复已确认原因后复验并恢复正式 Release 包，再独立验证用户登录及下载。不得运行会清空 Keychain 或创建测试账号的旧认证集成用例来诊断生产手机。

- [x] 真机复现 5 秒 connectionTimeout；30 秒有界预算下 readiness 200 / 空凭据登录 422 通过。
- [x] 将共享连接预算调整为 30 秒，不增加自动重试或证书例外；新增配置回归测试先红后绿。
- [x] 最终共享配置真机两项连通复验、正常 Release 签名构建及覆盖安装。
- [ ] 用户解锁后真实账号登录、文件保存及网络稳定性验收。

- [x] 对照所有业务页面和生成契约，记录功能与文案差异。
- [x] 修复认证、分页与筛选、资料修改、报告及管理功能。
- [x] 补充字段校验、分页参数、二进制导出与表单提交保护回归测试。
- [x] 验证 iOS 真实注册、账户修改和管理表单；浏览器检查桌面/390px 深浅主题。
- [x] 最终全量检查和双端构建结果写入验收。

测试环境使用已有 PostgreSQL 的隔离 schema、本地 SMTP 捕获器和合成账户；不触达真实用户邮件，不重建生产服务。

2026-09-12 CQ-001/CQ-005 增量：

- [x] 来源感知恢复动作、应用层逻辑操作 key、并发合并及会话隔离。
- [x] 故障/取消/文件缺失 Widget，响应丢失/并发/账号变更单测；全量 analyze、测试、Android debug 与 iOS simulator 构建。
- [ ] 真实 API 响应丢失后的同任务回放、真实手机保存与播放；不因组件或构建通过而勾选。

035 策略增量：

- [x] 同源 OpenAPI 策略/hosts/诊断，策略变化清除结果并更新幂等指纹，共享下拉组件单项禁用。
- [x] 管理诊断按需读取、失败重试和空状态；全量 201 项回归及 analyze。
- [ ] 本批新构建、实际服务解析/下载与真机保存播放；继续受服务端 035 门禁约束。
