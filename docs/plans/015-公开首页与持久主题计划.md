# 015 公开首页与持久主题计划

- 状态：Complete
- 日期：2026-08-31

1. 更新路由门禁，使 `/` 根据会话显示公开首页或下载工作区，其他资源路由保持认证保护。
2. 建立中英文公开首页和 390×844 Widget 回归。
3. 抽取全局一键主题按钮，覆盖公开、认证和登录后 AppBar。
4. 使用非敏感系统偏好保存浅色/深色选择，并增加状态单元测试。
5. 同步 README、文档索引、集成流程和退出落点。
6. 运行格式、分析、全量测试、Android/iOS 构建，并安装到连接的 iPhone 验收。

## 2026-09-03 Web 内容基线同步

7. 以 Web SEO 公开页为基线，补齐 App 的统一首屏标语、解析/选择/执行/交付链路和三类产品能力文案。
8. 抽取原生章节引导、工作链路、能力列表与安全边界组件，避免在页面文件中堆叠重复排版。
9. 集中管理源代码与 Quick Start 地址，补齐自托管技术栈、安全约束和本地部署入口。
10. 更新中英文 ARB 与 390×844 大字体回归，运行生成、格式、分析、全量测试和非 Debug 构建。

## 2026-09-21 Web / App 内容与排版同步

11. [x] 盘点 Web 与 App 路由、公开首页、帮助内容和管理数据页，确认 App 缺失的独立页面为 `/guide`。
12. [x] 将页面标题、正文、数据指标、加载、空状态与错误状态统一为左对齐，只保留导航和按钮内部的控件级居中。
13. [x] 使用 `shadcn_ui` 组件补齐 Web 首页当前的编号能力层级、FAQ 和使用指南入口，不引入第二套视觉组件。
14. [x] 新增 App `/guide` 公开路由和登录后设置入口，覆盖 AI 报告、剧本文档、自托管、Web / App 分工与 Provider 可用性。
15. [x] 修复会话恢复吞掉初始深链的问题，公开页保留原路由，受保护页恢复后返回原目标。
16. [x] 补齐内容合同、左对齐、大字体和深链回归，并在 iPhone 17 Pro / iOS 27 上完成运行态视觉验收。
