# 017 持久解析与原生恢复计划

依赖：服务端 044 P9.02／P9.07 已提供意图、历史、过期更新和唯一 job 交接；账号授权续接、全平台能力及 App 发布门禁未完成。输入为服务端运行的 `/openapi.json`，冻结到 `contracts/openapi/video-server.openapi.json`，生成目录仅由工具维护。

1. 评审 Design／PRD／Acceptance，纳入解析意图方法和响应，生成 Bearer 客户端并执行冻结契约检查。目标 FR-A1、NFR-A3。
2. 将公开链接入口迁到持久意图，历史用独立 owner 隔离与游标分页控制器，增加只读重入、前后台更新与身份清理；文章／上传／账号明确权限分支保持语义。目标 FR-A1、FR-A2、FR-A4、NFR-A1／A2。
3. 接入过期更新与重新确认、取消／响应丢失竞态测试、旧路径过期清理、失败原因与只读重试 Widget 状态覆盖。目标 FR-A3／A4、NFR-A1／A3。
4. 执行 App format／analyze／test、生成漂移检查、Android／iOS 构建与设备业务路径；与服务端版本一起核对发布。未获设备或授权样本时记录未验收，不关闭 P9.12。

工作区现有 015 首页／主题改动属于独立任务；只暂存本任务文件与可分离的本任务行。执行证据与限制更新到 017 Acceptance，并同步服务端 044 Plan P9.12。
