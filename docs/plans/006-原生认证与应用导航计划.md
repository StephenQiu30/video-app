# 006 原生认证与应用导航计划

- 状态：Ready
- 日期：2026-08-30

1. [ ] 冻结并生成 Server 026 App OpenAPI 客户端。
2. [ ] 先增加表单校验、完整注册/登录、恢复和退出 Widget/单元测试。
3. [ ] 实现安全 Session Store、生成 API Gateway 与 Riverpod 会话状态机。
4. [ ] 实现独立登录/注册路由及 signed-out/signed-in 账户页面。
5. [ ] 重构顶部品牌/会话栏与底部五 Tab Bar，保持大字体和屏幕阅读器语义。
6. [ ] 完成格式、分析、测试、Android/iOS 构建和模拟器视觉复验。
7. [ ] 使用 agent-browser 验收 Server Web 认证基线，并把边界与证据写入 Acceptance。

## 2026-09-08 评审修复

会话代次、串行凭据写入/清理、迟到授权撤销及请求重放边界已实现；认证并发回归、双端构建及模拟器真实集成通过。完整结果见 [本轮验收](../acceptance/006-原生认证与应用导航验收.md)。

## 2026-09-08 邮箱验证注册

用户确认保留邮箱＋密码登录，注册必须验证邮箱。App 使用生成的 `sendNativeRegistrationCode` 获取验证码，再通过 `registerNativeUser` 提交 `verification_code`；验证码不持久化。界面提供发送、60 秒重发冷却、邮箱修改后清空、过期/错误和邮件不可用反馈。SMTP 由 Server 管理，App 不读取邮件凭据。

计划状态：Ready；按现有原生 Token 契约扩展，沿用 Server 033 验证规则。
