# 007 无边框导航与认证门禁计划

- 状态：Complete
- 日期：2026-08-30

1. 保存用户选定视觉稿，冻结无边框与无重复入口规则。
2. 引入兼容本机 Flutter 的最新稳定 `lucide_icons_flutter`，替换主导航与内容 Tabs 图标。
3. 重构 `DownloadAppBar`、`ContentIntakeSelector` 与 `AppBottomNavigation`。
4. 增加会话恢复路由和基于 `AuthSessionPhase` 的 go_router 重定向。
5. 调整登录/注册返回行为和退出后的落点。
6. 更新中英文短 Tab 标签、Widget 测试与真实集成测试。
7. 运行格式、生成、分析、测试、真实 iOS 集成和 Android/iOS 构建。
8. 在 390×844 模拟器捕获实现图，与选定稿对比并完成 `design-qa.md` 门禁。
