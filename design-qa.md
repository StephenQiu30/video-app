# 017 下载记录统计与滑动操作 Design QA

## 对照目标

- 视觉事实：`/Users/stephenqiu/Downloads/截屏 2026-09-01 13.35.34.png`。
- 实现截图：`qa-output/017-history-swipe/implementation-default.png`。
- 全屏同画布对照：`qa-output/017-history-swipe/comparison.png`。
- 聚焦统计与列表对照：`qa-output/017-history-swipe/focused-summary-list.png`。
- 视口：iPhone 16，逻辑尺寸 393×852，设备像素比 3。
- 像素：来源与实现均为 1179×2556；未缩放、未做密度换算。同画布对照为 2358×2556，聚焦对照为两侧各裁切 `x=0, y=750, w=1179, h=1500` 后横向拼接。
- 状态：浅色主题、下载记录页、统计与列表默认态。来源使用用户真实记录，实现使用隔离的非用户测试数据，因此只比较布局、密度、层级、状态表达与操作可见性；原生状态栏中的灵动岛差异不属于 App 内容。

## Findings

没有剩余 P0、P1 或 P2 问题。

- 字体与层级：页面标题、说明、列表标题、元数据和状态文字继续复用既有主题，没有改变字号或字重。长标题保持两行省略，未挤压状态信息。
- 间距与布局：四项统计在 393pt 宽度内改为四等分单行居中；数值和标签使用相同中心轴，完整占满内容宽度。列表随统计区收紧上移，但标题区、分割线和底部导航节奏保持不变。
- 色彩与 Token：默认列表继续使用页面背景色与现有分割线，未引入卡片边框。危险色只出现在滑动后展示的删除操作中，默认态不再持续显示红色垃圾桶。
- 图片质量与资产：生产列表仍复用 `AuthenticatedMediaCover`，没有替换封面加载、裁切或鉴权路径。隔离截图故意使用现有“封面不可用 / 生成中”状态，避免把用户媒体写入仓库。
- 文案与内容：页面标题、说明、统计标签、任务状态和底部导航与现有 Web/App 文案保持一致；新增读屏提示为“向左轻扫可查看任务操作”。
- 操作与可发现性：向左滑动通过 `Slidable` 展示“任务详情”和“删除任务”；执行中任务额外展示“取消任务”，失败或取消任务额外展示“重新下载”。删除仍必须经过原有不可撤销确认，不支持整行一滑即删。
- 可访问性：每一行保留 button 语义、完整标题和状态，并通过自定义语义操作暴露详情、取消/重试和删除，因此不依赖手势才能完成操作。文本缩放超过 1.25 倍或可用宽度不足 340pt 时，统计自动回落为两列。

## 聚焦对照

- `focused-summary-list.png` 中，来源左侧的 2×2 左对齐统计产生大面积空白；实现右侧的四等分布局在同一水平线上均匀填满宽度，列表更早进入首屏。
- 来源列表每行右侧持续显示红色删除图标；实现默认态只保留中性的详情箭头，降低误触与危险操作的视觉权重。
- 两侧采用同一 393×852 逻辑视口和 3× 像素密度，统计与列表细节在聚焦图中可直接读取，无需额外缩放判断。

## 比较历史

1. 初始证据：统计为 2×2 左对齐且留白过多；删除按钮在每一行持续高亮，破坏视觉层级并增加误触风险。
2. 第一轮实现：四等分居中统计；列表接入滑动操作区；删除、取消和重试继续调用现有真实 Repository 操作与确认流程。
3. 第一轮视觉检查发现列表行为了覆盖滑动操作使用 `surface`，形成轻微灰色块面，与既有无边框页面有漂移。
4. 修复：列表行背景改回 `scaffoldBackgroundColor`，保留不透明点击层但恢复页面连续背景。
5. 后验同画布与聚焦对照未发现可执行的 P0、P1 或 P2 问题；统计密度、默认删除可见性、行高、分割线和底部导航均符合目标。

## Primary interactions checked

- 默认态删除操作不可命中。
- 向左滑动下载行后，“删除任务”操作可命中。
- 取消确认后任务不会删除；再次滑动并确认后调用当前用户任务删除接口。
- 点击未展开的下载行仍进入任务详情。
- 自动化验证 393pt 宽度下四项统计同高、等间距并以屏幕中心对称。
- Simulator 开发构建未出现 Flutter 框架异常；滑动组件滚动时会自动关闭，多个列表行只保留一个展开操作区。

## Follow-up polish

没有阻断项。P3 可选项：当后端未来提供可编辑字段时，可在同一个滑动操作区增加真实“编辑”能力；当前下载记录是不可变任务数据，因此没有添加无法保存的假编辑入口。

final result: passed
